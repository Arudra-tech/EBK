# Copy to dashboard/.env.local (gitignored by Vite convention) and restart `npm run dev`.
# On the GB10 the NemoClaw vLLM container owns host port 8000, so the EBK backend runs on 8001.
VITE_API_URL=http://localhost:8001
VITE_WS_URL=ws://localhost:8001/ws
