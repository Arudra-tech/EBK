[project]
name = "ebk"
version = "0.1.0"
description = "EBK — local AI deployment optimization agent (Dell x NVIDIA hackathon)"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "pymongo>=4.13",
    "httpx>=0.27",
]

[tool.uv]
package = false
